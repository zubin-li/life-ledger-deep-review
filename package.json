{
  "name": "life-ledger-deep-review",
  "version": "1.1.4",
  "description": "A calm, multilingual, local-first habit tracker and deep-review PWA.",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zubin-li/life-ledger-deep-review.git"
  },
  "homepage": "https://github.com/zubin-li/life-ledger-deep-review#readme",
  "bugs": {
    "url": "https://github.com/zubin-li/life-ledger-deep-review/issues"
  },
  "scripts": {
    "predev": "wrangler d1 migrations apply DB --local",
    "dev": "wrangler dev",
    "build:cloudbase-sdk": "node scripts/build-cloudbase-sdk.mjs",
    "build:heic-vendor": "node scripts/build-heic-vendor.mjs",
    "build:cloudbase": "npm run build:cloudbase-sdk && node scripts/build-cloudbase.mjs",
    "deploy:cloudbase": "node scripts/deploy-cloudbase.mjs",
    "check": "node --check public/app.js && node --check public/focus-timer.js && node --check public/voice-checkin.js && node --check public/photo-memories.js && node --check public/media-backup.js && node --check public/cloudbase-sync.js && node --check public/sw.js && node --check src/calendar.js && node --check src/photos.js && node --check src/voice.js && node --check src/index.js && node scripts/check.mjs",
    "test": "npm run check && node --test tests/*.test.mjs",
    "db:migrations:apply": "wrangler d1 migrations apply DB --remote",
    "deploy": "npm run db:migrations:apply && wrangler deploy"
  },
  "cloudflare": {
    "bindings": {
      "DB": {
        "description": "Your private Life Ledger D1 database. Cloudflare creates and binds it during deployment."
      },
      "PHOTOS": {
        "description": "A private R2 bucket for compressed daily photo memories."
      }
    }
  },
  "dependencies": {
    "@cloudbase/js-sdk": "^3.8.0",
    "heic2any": "0.0.4",
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "wrangler": "^4.122.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
